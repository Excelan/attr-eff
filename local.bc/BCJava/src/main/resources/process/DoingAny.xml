<process prototype="DMS:Execution:Doing" readyfor="test">

    <!-- Исполнение -->

    <parameters>
        <param name="initiator" type="uun"  />
        <param name="subject" type="urn"  /> <!-- передача из вызывающего процесса, что визировать -->
    </parameters>

    <responsibility>
        <stage name="DoingTask" title="Исполнение">
            <humantask responsible="doer"  appoint="configuration" />
            <rule type="timelimit" value="1d" />
        </stage>
    </responsibility>

    <stages>

    <!-- subject -->
    <stage name="DoingTask" type="humantask">
        <!-- ? -->
    </stage>

    </stages>

</process>
